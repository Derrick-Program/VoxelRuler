import { AppTheme } from "@theme";
export component Home inherits Rectangle {
    Text {
        text: "Home Page";
        font-size: 24px;
        font-weight: 700;
        color: AppTheme.colors.foreground;
    }
}